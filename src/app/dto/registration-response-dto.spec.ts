import { RegistrationResponseDto } from './registration-response-dto';

describe('RegistrationResponseDto', () => {
  it('should create an instance', () => {
    expect(new RegistrationResponseDto()).toBeTruthy();
  });
});
